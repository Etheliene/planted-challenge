import React, { useState } from "react";

type SearchBarProps = {
    searchValue: string
}

const SearchBar = (props: SearchBarProps) => {
    const { searchValue } = props;

    return (
        <React.Fragment>
            xx
        </React.Fragment>
    )
}